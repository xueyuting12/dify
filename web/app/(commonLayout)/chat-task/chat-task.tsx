'use client'

import React from 'react'
import type { ITaskItem } from '@/service/chatTask'

type IChatTaskProps = {
  list: ITaskItem[]
}

const ChatTask = ({
  list,
}: IChatTaskProps) => {
  return (
    <div>

    </div>
  )
}

export default ChatTask
